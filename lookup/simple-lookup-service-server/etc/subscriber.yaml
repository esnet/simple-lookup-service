#All caches are read-only
caches:
    - name: "cache"
      type: "replication"
      publishers:
        - locator: "http://ps-west.es.net:8090/lookup/subscribe"
          queries:
            - "*"
bootstrapserver: "http://ps1.es.net:8095/lookup/bootstrap"

