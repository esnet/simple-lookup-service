caches:
    - name: "cache"
      type: "replication"
      sources:
        - locator: "http://antg-test-vm.es.net:8090/lookup/records"
          queries:
            - type: "service"

bootstrapserver: "http://localhost:8090/lookup/bootstrap"

