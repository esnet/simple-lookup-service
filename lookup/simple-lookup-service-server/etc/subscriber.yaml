#All caches are read-only
caches:
    - name: "cache"
      type: "replication"
      publishers:
        - locator: "http://localhost:8090/lookup/subscribe"
          queries: 
              - "*"
bootstrapserver: "http://ps-west.es.net/lookup/activehosts.json"
reconnectInterval: 1800
