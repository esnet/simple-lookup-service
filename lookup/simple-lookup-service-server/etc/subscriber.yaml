caches:
    - name: "cache"
      type: "replication"
      sources:
        - locator: "http://localhost:8090/lookup/bootstrap"
          queries:
            - type: "service"

bootstrapserver: "http://localhost:8090/lookup/bootstrap"

