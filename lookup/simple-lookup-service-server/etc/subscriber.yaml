#All caches are read-only
caches:
    - name: "cache"
      type: "replication"
      publishers:
        - locator: "http://localhost:8090/lookup/subscribe"
          queries:
            - type: "service"

